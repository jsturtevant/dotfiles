{
    "tasks": [
    {
        "label": "containerd",
        "type": "shell",
        "command": "go",
        "args": [
            "build",
            "-gcflags=all=-N -l",
            "-o",
            "${workspaceFolder}/bin/containerd"
        ],
        "options": {
            "cwd": "${workspaceFolder}/cmd/containerd"
        },
    }
]
}